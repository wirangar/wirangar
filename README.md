# Bazarino Telegram Bot

A Telegram bot for managing orders with payment integration (Stripe) and Google Sheets storage.

## Setup

1. **Install dependencies**:
   ```bash
   pip install -r requirements.txt